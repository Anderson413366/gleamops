/**
 * /clients/sites/[id] — renders the same site detail page as /crm/sites/[id].
 * The component is imported directly from the CRM directory to avoid duplication.
 */
export { default } from '../../../crm/sites/[id]/page';
